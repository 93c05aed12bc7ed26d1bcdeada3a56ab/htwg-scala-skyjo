
package de.htwg.se.skyjo.view

import org.scalatest._

class TuiSpec extends WordSpec with Matchers {
  /*  "A Tui" when { "new" should {
      val player = new Player("Hans", new Deck())
      val tui = new Tui(new Controller(new Deck(),player), player)

      "do nothing on input 'q'" in {
        tui.processInput("q")
      }
      "move cursor to the right" in {
        val x = player.hand.posX
        tui.processInput("d")
        player.hand.posX should be > x
      }
      "move cursor to the left" in {
        val x = player.hand.posX
        tui.processInput("a")
        player.hand.posX should be < x
      }
      "move cursor down" in {
        val y = player.hand.posY
        tui.processInput("s")
        player.hand.posY should be > y
      }
      "move cursor up" in {
        val y = player.hand.posY
        tui.processInput("w")
        player.hand.posY should be < y
      }
      "uncover card" in {
        tui.processInput("e")
        player.hand.cards(player.hand.posY)(player.hand.posX).getValue should not be "#"
      }
      "do nothing at any other input" in {
        tui.processInput("asdf")
      }
    }}*/
}